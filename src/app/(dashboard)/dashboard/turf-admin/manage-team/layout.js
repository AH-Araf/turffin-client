export const metadata = {
  title: "Manage team | TurfAdmin | Turffin",
  description: "Manage staff accounts, access roles, and permissions for your turf organization.",
};

export default function Layout({ children }) {
  return children;
}
