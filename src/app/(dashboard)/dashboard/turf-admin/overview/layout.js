export const metadata = {
  title: "Overview | TurfAdmin | Turffin",
  description: "Facility performance overview for TurfAdmin.",
};

export default function Layout({ children }) {
  return children;
}
