export const metadata = {
  title: "Booking requests | TurfAdmin | Turffin",
  description: "Review and approve incoming turf reservation requests.",
};

export default function Layout({ children }) {
  return children;
}
