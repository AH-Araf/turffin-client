export const metadata = {
  title: "Calendar | TurfAdmin | Turffin",
  description: "Weekly booking schedule for your facility on Turffin.",
};

export default function Layout({ children }) {
  return children;
}
