import { LoginView } from "./LoginView";

export const metadata = {
  title: "Login | Turffin",
  description: "Sign in to Turffin to manage your turf bookings, teams, and account.",
};

export default function LoginPage() {
  return <LoginView />;
}
