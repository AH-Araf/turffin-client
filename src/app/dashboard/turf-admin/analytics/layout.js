export const metadata = {
  title: "Analytics | TurfAdmin | Turffin",
  description: "Performance analytics for your turf facility on Turffin.",
};

export default function Layout({ children }) {
  return children;
}
