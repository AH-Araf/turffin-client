# Public API base URL (must include /api — backend mounts routes at /api).
# Copy to .env.local for local dev; never put secrets here — NEXT_PUBLIC_* is exposed in the browser.
NEXT_PUBLIC_API_BASE_URL=http://localhost:5050/api
